{% comment %}
  To write a <svg> element, use this template.
  `id` is the required identifier of the icon in the sprite
{% endcomment %}
<svg class="svgcon">
  <use xlink:href="#{{ include.id }}" />
</svg>